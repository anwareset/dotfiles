#!/bin/bash

#amixer -Mq set Master,0 5%- unmute && pkill dunst && notify-send -u low 'Volume Down ' $(volume)
amixer -Mq set Master,0 5%- unmute && notify-send -u low '🕩 Volume Down' $(volume) -h string:x-canonical-private-synchronous:volume

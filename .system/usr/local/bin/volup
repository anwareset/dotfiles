#!/bin/bash

#amixer -Mq set Master,0 5%+ unmute && pkill dunst && notify-send -u low 'Volume Rise ' $(volume)
amixer -Mq set Master,0 5%+ unmute && notify-send -u low ' Volume Rise' $(volume) -h string:x-canonical-private-synchronous:volume
